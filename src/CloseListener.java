/**
 * CMS270 Group Project
 * @version 1.0.0
 * @authors Jarod Fuentes, Ava Kopchak, Alex Rynott, Malcolm Stone
 * 
 * @purpose create an event handler for the on close event. THis event handler is called when the user closes a window via the close btn
 */

public interface CloseListener {
    void onClose(boolean dicAge, boolean dicGender);
}
